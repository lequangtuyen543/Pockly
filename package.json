{
  "name": "pockly",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@8.8.0",
  "workspaces": [
    "pockly-client",
    "pockly-server"
  ],
  "scripts": {
    "dev": "pnpm --filter pockly-client dev",
    "build": "pnpm --filter pockly-client build",
    "test": "pnpm --filter pockly-client test"
  }
}
